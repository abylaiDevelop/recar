version: '3.7'
services:
  postgres:
    image: postgres
    container_name: postgresdb
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: diploma
    ports:
    - 5433:5432
    volumes:
    - "./data/diploma"

volumes:
  data: {}